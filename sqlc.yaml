version: "2"

sql:
  - engine: "postgresql"
    schema: "db/migrations/"
    queries: "db/sqlc/queries/users.sql"
    gen:
      go:
        package: "generated" # ✅ must match your import
        out: "db/sqlc/generated"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
